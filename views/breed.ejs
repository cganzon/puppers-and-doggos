<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= breedDetails.name %></title>
</head>
<body>
    <% for(let breed of breedInfo) { %>
        <h2><%= breedDetails.name %></h2>
        <% if(breedDetails.origin) { %>
            <p>Origin: <%= breedDetails.origin %></p>
        <% } %>
        <% if(breedDetails.breed_group) { %>
            <p>Breed group: <%= breedDetails.breed_group %></p>
        <% } %>
        <% if(breedDetails.bred_for) { %>
            <p>Bred for: <%= breedDetails.bred_for %></p>
        <% } %>
        <p>Life span: <%= breedDetails.life_span %></p>
        <p>Weight: <%= breedDetails.weight.imperial %> lb.</p>
        <p>Height: <%= breedDetails.height.imperial %> in.</p>
        <p>Personality: <%= breedDetails.temperament %></p>
        <img src="<%= breed.url %>" alt="<%= breedDetails.name %>">
    <% } %>
    
</body>
</html>